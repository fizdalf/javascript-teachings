//create a new function that returns a square with a drawing inside.

const {Pixel} = require("./mixed.exercise1.solutions")
const {Square} = require("./mixed.exercise3.solutions")

const canvas = Square(10, 10, Pixel(255, 0, 0, 255), Pixel(0, 0, 0, 0))

function smileyFace (square, )
