//Las variables son igual que las constantes pero puedes cambiarle el valor inicial

let dollarsInMyAccount = 0;
dollarsInMyAccount = 500

//Se cambia el valor a 500

let conter = 0
counter = counter + 1; //1
counter = counter + 1; //2
counter = counter + 1; //3

//Estamos pidiendo que coja el valor de counter y le sume 1

counter ++; //counter = counter +1
counter --; //counter = counter -1
counter += 2; //counter = counter + 2
counter -= 2; //counter = counter - 2
counter *= 2; //counter = counter * 2
counter /= 2; //counter = counter / 2