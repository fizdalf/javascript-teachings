//8 2 6 9 10 5


const arguments = process.argv
const lastIndex = arguments.length - 1

while (index <= lastIndex) {
    let index = 2;
    const listaDeNotas = parseInt(arguments[index])
    let media = []
    listaDeNotas.push = media
    index++
}
const lastMedia = media.length - 1

while (indexMedia <= lastMedia) {
    let suma = 0
    let indexMedia = 0;
    (indexMedia + suma = total)

    suma = total
    indexMedia++
}
console.log("la media es " + suma / 2)
