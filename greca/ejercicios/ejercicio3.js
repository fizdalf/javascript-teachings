// Crea un programa que si el valor de la variable "numero" es divisible entre 2, imprime por terminal
// "Es divisible entre 2" y luego si es divisible entre 5, imprime por terminal "Es divisible entre 5"

let numero = 12;
if (numero % 2 === 0) {
    console.log("Es divisible entre 2")
}
if(numero % 5 === 0) {
    console.log ("Es divisible entre 5")
}